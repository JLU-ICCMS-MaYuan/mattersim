# 模拟准备

## 2025-11-27T12:53:16+08:00
- 用户需求：在提交现有变更（排除 `deepmd-kit/`）后，为 `tests/applications/LaH10-md/` 准备 300 K、150 GPa 的 NPT 模拟所需全部文件，基于提供的 `std_H10La_Fm-3m_225_.vasp`，且需记录 Langevin 控制与新增日志分类。
- 助手响应：说明将先更新 AGENTS 与日志，再查阅仓库文档确定 MD 方法，再准备结构与脚本，最后复核并按要求提交；提交信息遵循 `[YYYY-MM-DD HH:MM:SS] <需求类型>：<摘要>` 模版。
- 当前计划：1) 确认提交与记录范围；2) 梳理文档和日志更新；3) 调研 MD 流程；4) 产出 LaH10 模拟文件；5) 复核并提交。

## 2025-11-27T13:00:29+08:00
- 操作概述：更新 AGENTS.md 记录本次交互要点，新增 `codexmd/模拟准备.md` 追踪，大量查阅 `src/mattersim/cli/applications/moldyn.py` 等文档；在 `tests/applications/LaH10-md/` 写入 `md_config.yaml`、`run_md.py` 与中文 README，内含 Langevin 温控+各向同性体积缩放策略、输出文件说明与执行命令。
- MD 脚本要点：自动依据配置加载 `MatterSimCalculator`，设置 0.5 fs 步长 Langevin，按压力偏差对晶胞施加缩放，周期性写入日志、轨迹、CSV、restart 与 summary。
- 后续动作：待确认后执行 `git add`（排除 `deepmd-kit/`），按照指定格式提交，并提示用户运行脚本/核对输出。若还需额外后处理或参数调整，将继续在此日志中补充记录。
